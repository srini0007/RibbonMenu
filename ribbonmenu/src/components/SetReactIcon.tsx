import React from 'react'

function SetReactIcon({children}:any) {
  return (
    <div className="flex items-center justify-center mb-4">
        {children}
      </div>
  )
}

export default SetReactIcon